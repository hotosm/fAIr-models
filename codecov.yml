codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        paths:
          - fair/
    patch:
      default:
        target: auto
        threshold: 1%
        paths:
          - fair/

flag_management:
  default_rules:
    carryforward: true
