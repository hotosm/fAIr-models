apiVersion: v1
kind: Service
metadata:
  name: zenml
  namespace: fair
  labels:
    app: zenml
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: zenml
