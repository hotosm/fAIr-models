stack_name: docker
zenml_version: 0.93.3
components:
  orchestrator:
    name: docker_orchestrator
    flavor: local_docker
    type: orchestrator
    configuration: {}
  artifact_store:
    name: docker_artifacts
    flavor: local
    type: artifact_store
    configuration: {}  # Uses default local_stores path
